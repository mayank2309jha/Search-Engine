# Build directory
build/

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
compile_commands.json

# Executables
*.out
*.exe
search_engine

# Object files
*.o
*.obj

# IDE
.vscode/
.idea/

# Python (crawler/)
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Crawler's generated data (crawl DB, raw HTML) — not source, not versioned
crawler/data/

# Symlinked to crawler/data/crawler.db — not versioned here
data/crawler.db

# Build artifact, regenerated from crawler.db by the C++ engine -- matches
# upstream (origin/main deleted this from tracking the same way). Currently
# 115MB+ and only getting bigger with corpus size; a committed binary blob
# that size doesn't belong in git history. Share via object storage instead.
data/index.bin